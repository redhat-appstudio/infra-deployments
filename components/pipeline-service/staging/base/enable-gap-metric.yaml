---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pipeline-metrics-exporter
  namespace: openshift-pipelines
spec:
  template:
    spec:
      containers:
        - name: pipeline-metrics-exporter
          env:
            - name: ENABLE_GAP_METRIC
              value: "true"
