apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: dev-sandbox-host
  namespace: toolchain-host-operator
spec:
  config:
    resources:
      requests:
        memory: 800Mi
        cpu: 100m
