apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: dev-sandbox-member
  namespace: toolchain-member-operator
spec:
  config:
    resources:
      requests:
        memory: 1.2Gi
        cpu: 100m
