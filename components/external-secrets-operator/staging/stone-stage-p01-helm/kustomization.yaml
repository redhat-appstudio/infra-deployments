apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: external-secrets-operator
resources:
  # Custom resources that match OLM deployment configuration
  # These ensure exact label matching for ServiceMonitor and metrics Service
  - custom-resources.yaml

