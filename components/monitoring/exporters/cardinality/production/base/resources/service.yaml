---
apiVersion: v1
kind: Service
metadata:
  name: prometheus-cardinality-exporter
  labels:
    app: prometheus-cardinality-exporter
spec:
  selector:
    app: prometheus-cardinality-exporter
  ports:
    - name: http
      port: 9090
      targetPort: http
