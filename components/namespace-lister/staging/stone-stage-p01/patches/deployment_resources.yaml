apiVersion: apps
kind: Deployment
metadata:
  name: namespace-lister
  namespace: namespace-lister
spec:
  template:
    spec:
      containers:
      - name: namespace-lister
        resources:
          limits:
            memory: 500MiB
          requests:
            memory: 500MiB
