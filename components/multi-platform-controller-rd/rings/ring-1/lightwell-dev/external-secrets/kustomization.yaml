apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- aws-account-es.yaml
- aws-ssh-key-es.yaml

# Previously commented-out (not deployed):
# aws-win-ssh-key, ibm-ppc64le-ssh-key, ibm-s390x-ssh-key
# vault prefix: staging/infrastructure/multi-platform-controller/lightwell-dev/
