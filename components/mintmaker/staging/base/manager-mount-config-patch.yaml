apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        volumeMounts:
        - name: controller-config
          mountPath: /etc/mintmaker
      volumes:
      - name: controller-config
        configMap:
          name: controller-config
