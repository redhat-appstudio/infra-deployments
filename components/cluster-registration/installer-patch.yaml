apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: installer
        securityContext:
          readOnlyRootFilesystem: true
          runAsNonRoot: true
