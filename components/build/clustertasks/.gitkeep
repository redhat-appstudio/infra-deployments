# This directory will contain Red Hat Build specific ClusterTasks
# Example, 
# 1. remote builds Cluster Tasks.
# 2. Runtime-specific s2i build Tasks with image signature & provenance compatibility.
# 3. Runtime-specific s2i build Tasks with image signature & provenance support.
# 4. SPI Task to get user credential before a build starts.

# Ignore everything in this directory
*
# Except this file
!.gitignore
