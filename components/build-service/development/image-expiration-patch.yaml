apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-expiration
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
          - name: BUILD_SERVICE_IMAGE_TAG_EXPIRATION
            value: ""
