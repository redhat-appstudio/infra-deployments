apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-studio-default-workspace
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
