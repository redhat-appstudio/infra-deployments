apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- https://github.com/konflux-ci/tekton-kueue/config/default?ref=f173be2d2ca02caf1caee684d9eedb30cb3a5198

images:
- name: konflux-ci/tekton-kueue
  newName: quay.io/konflux-ci/tekton-kueue
  newTag: f173be2d2ca02caf1caee684d9eedb30cb3a5198

namespace: tekton-kueue
# ensure that installation starts after the installation of kueue complete
commonAnnotations:
  argocd.argoproj.io/sync-wave: "10"

configMapGenerator:
  - name: tekton-kueue-config
    namespace: tekton-kueue
    behavior: replace
    files:
      - config.yaml

generatorOptions:
  disableNameSuffixHash: true

patches:
  # Development: 1 replica so sync/operator does not revert scale-down (staging/prod use 2+).
  - path: controller-patch.yaml
    target:
      group: apps
      kind: Deployment
      name: tekton-kueue-controller-manager
      version: v1
  - path: webhook-patch.yaml
    target:
      group: apps
      kind: Deployment
      name: tekton-kueue-webhook
      version: v1
  - target:
      kind: Namespace
      name: tekton-kueue
    patch: |-
      - op: add
        path: /metadata/labels/openshift.io~1cluster-monitoring
        value: "true"
