apiVersion: v1
kind: Pod
metadata:
  name: test-remote-platform
spec:
  containers:
  - name: step-build
    resources:
      requests:
        cpu: "1"
        memory: 4Gi
      limits:
        cpu: "1"
        memory: 4Gi
