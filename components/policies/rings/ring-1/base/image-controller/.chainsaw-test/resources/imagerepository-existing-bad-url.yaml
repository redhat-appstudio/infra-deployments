apiVersion: konflux-ci.dev/v1alpha1
kind: ImageRepository
metadata:
  name: test-existing-bad-url
spec:
  image:
    name: my-tenant/test
    visibility: public
  notifications:
    - config:
        url: 'https://legacy-invalid.example.com/webhook'
      event: repo_push
      method: webhook
      title: legacy-webhook
