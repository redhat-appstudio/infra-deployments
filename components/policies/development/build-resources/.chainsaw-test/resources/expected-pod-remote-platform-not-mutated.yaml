apiVersion: v1
kind: Pod
metadata:
  name: test-remote-platform
spec:
  containers:
  - name: step-build
    resources:
      requests:
        cpu: "4"
        memory: 8Gi
      limits:
        cpu: "4"
        memory: 8Gi
