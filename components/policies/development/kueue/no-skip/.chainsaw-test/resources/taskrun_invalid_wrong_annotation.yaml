apiVersion: tekton.dev/v1
kind: TaskRun
metadata:
  name: invalid-wrong-annotation-task
  annotations:
    kueue.konflux-ci.dev/resource-another-platform: "1"
spec:
  taskSpec:
    steps:
    - name: no-op
      image: registry.access.redhat.com/ubi10-micro:10.1
      script: |-
        #!/bin/bash
        echo "no-op"
    params:
    - description: The platform to build on
      name: PLATFORM
      type: string
  params:
  - name: PLATFORM
    value: test-platform
