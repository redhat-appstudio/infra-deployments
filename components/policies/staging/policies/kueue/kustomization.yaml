apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../../development/kueue/
# this is temporary. we need this to rollout a new version of the ClusterPolicy
# that changes an immutable field.
patchesStrategicMerge:
- delete-queue-config-clusterpolicy.yaml
