apiVersion: v1
kind: ConfigMap
metadata:
  namespace: konflux-policies
  name: macos-allowlist
data:
  # To allow a namespace to use macos runners, add their name here:
  namespaces: |
    red-hat-desktop-tenant
