---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: monitor-release-pipeline-role
rules:
  - apiGroups:
      - tekton.dev
      - ""
    resources:
      - pipelineruns
      - pipelines
      - pods
      - pods/logs
    verbs:
      - get
      - list
      - watch
