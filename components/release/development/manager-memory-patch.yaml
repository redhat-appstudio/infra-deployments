apiVersion: apps/v1
kind: Deployment
metadata:
  name: release-service-controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        resources:
          limits:
            memory: 256Mi
