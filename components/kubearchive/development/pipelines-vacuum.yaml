---
apiVersion: kubearchive.org/v1
kind: ClusterVacuumConfig
metadata:
  name: pipelines-vacuum-config
spec:
  namespaces:
    ___all-namespaces___:
      resources:
        - apiVersion: tekton.dev/v1
          kind: PipelineRun
---
apiVersion: batch/v1
kind: CronJob
metadata:
  annotations:
    # Needed if just the command is changed, otherwise the job needs to be deleted manually
    argocd.argoproj.io/sync-options: Force=true,Replace=true
  name: pipelines-vacuum
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: kubearchive-cluster-vacuum
          containers:
            - name: vacuum
              image: quay.io/kubearchive/vacuum:v1.6.0
              command: [ "/ko-app/vacuum" ]
              args:
                - "--type"
                - "cluster"
                - "--config"
                - "pipelines-vacuum-config"
              env:
                - name: KUBEARCHIVE_NAMESPACE
                  valueFrom:
                    fieldRef:
                     fieldPath: metadata.namespace
              securityContext:
                readOnlyRootFilesystem: true
                runAsNonRoot: true
              resources:
                requests:
                  cpu: 200m
                  memory: 2Gi
                limits:
                  cpu: 200m
                  memory: 2Gi
          restartPolicy: Never
