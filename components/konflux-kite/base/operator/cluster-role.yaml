apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kite-bridge-operator-manager-role
rules:
- apiGroups:
  - tekton.dev
  resources:
  - pipelineruns
  verbs:
  - get
  - list
  - watch
